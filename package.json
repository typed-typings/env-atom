{
  "private": true,
  "scripts": {
    "watch": "tsc -w",
    "build": "tsc",
    "test": "apm test",
    "retest": "npm run build && npm test",
    "tdd": "onchange 'spec/**/*.ts' -- npm run retest"
  },
  "devDependencies": {
    "onchange": "^2.2.0",
    "tslint": "^3.10.2",
    "tslint-config-typings": "^0.2.3",
    "typings": "^1.0.4"
  }
}
