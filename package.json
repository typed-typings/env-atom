{
  "private": true,
  "scripts": {
    "build": "typings bundle -A -o spec",
    "test": "tsc -p spec && apm test"
  },
  "devDependencies": {
    "tslint": "github:unional/tslint",
    "tslint-config-typings": "^0.1.0",
    "typescript": "^1.8.9",
    "typings": "^0.7.9"
  }
}
